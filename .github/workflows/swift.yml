name: Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: Build macOS
      run: xcodebuild build -scheme Components -destination platform="macOS"
    - name: Build iOS
      run: xcodebuild build -scheme Components-iOS -destination platform="iOS Simulator,name=iPhone 11 Pro"
